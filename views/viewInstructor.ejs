<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">

    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://fonts.googleapis.com/css2?family=Oleo+Script&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <title>Instructor dashboard</title>
</head>

<body>
    <%- include('instructorNavbar') %>
        <!-- main body section -->
        <div class="header">
            <h2>Personal Details</h2>
        </div>
        <div class="sub-header">
            <h5> <b>Full name : </b>
                <%= instructor.fullname %>
            </h5>
            <% var options={ weekday: 'long' , year: 'numeric' , month: 'long' , day: 'numeric' }; %>
                <h5> <b>Date of Birth : </b>
                    <%= instructor.dob.toLocaleDateString("en-US", options) %>
                </h5>
                <h5> <b>Gender : </b>
                    <%= instructor.gender %>
                </h5>
                <h5> <b>Mobile Number : </b>
                    <%= instructor.mobileNO %>
                </h5>
                <h5> <b>Personal Email : </b>
                    <%= instructor.myemail %>
                </h5>
                <h5> <b>Profile picture : </b>
                    <% if (typeof(instructor.profilePicture)==='undefined') { %>
                        <img src="img/dp.PNG" alt="">
                    <% } else { %>
                        <img src="data:<%= instructor.profilePicture.contentType %>;base64,<%= instructor.profilePicture.toString('base64') %>" alt="Profile Picture">
                    <% } %>
                </h5>
        </div>
        <div class="header">
            <h2>Academic Details</h2>
        </div>
        <div class="sub-header">
            <br>
            <h5> <b>College Email : </b>
                <%= instructor.email %>
            </h5>
            <h5>
                <b>Date of joining : </b>
                <% if (!typeof(instructor.profilePicture)==='undefined') { %>
                    <%= instructor.joiningDate.toLocaleDateString("en-US", options) %>
                    <% } %>  
            </h5>
        </div>
</body>

</html>