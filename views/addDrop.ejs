<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">

    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://fonts.googleapis.com/css2?family=Oleo+Script&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/x-icon" href="/img/favicon.png">

    <title>Add Drop courses</title>
</head>

<body>
    <%- include('adminNavbar') %>
    <div class="header">
        <h2>Add - Drop courses</h2>
    </div>
    <section class="home-section">
        <div class="table-container" style="margin:10%; margin-top: 0;">
            <ul class="table-responsive-lg" >
<form action="/addDropAdmin" method="post">

    <div class="data-container" >
            <p class="textfield"><strong>Allow registered students of <%= semester[0].name %> to add and drop courses</strong></p>
                <% if (semester[0].addDrop==true) { %>
                    <input style="accent-color: #242728; transform:scale(1.5); margin-left:8%; margin-right:10px;" type="checkbox" name="addDrop" id="addDrop" checked>
                    <span>Uncheck the checkbox to close add-drop period.</span>
                    <br>
                    <br>
                        <div style="display:flex; justify-content:center; " class="input-field">
                            <button  style="width:30%;" class="submit" type="submit">Close Add-Drop Period</button>
                        </div>
                   
                    <% } else { %>
                        <input style="accent-color: #242728; transform:scale(1.5); margin-left:8%; margin-right:10px;" type="checkbox" name="addDrop" id="addDrop">
                        <span>Check the checkbox to open add-drop period.</span>
                        <br>
                        <br>
                        <div style="display:flex; justify-content:center; " class="input-field">
                            <button  style="width:30%;" class="submit" type="submit">Open Add-Drop Period</button>
                        </div>
                        
                    <% } %> 
    </div>
    </form>
    </ul>
</div>
</section>
   
</body>